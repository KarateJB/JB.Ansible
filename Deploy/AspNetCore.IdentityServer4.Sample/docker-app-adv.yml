---
- name: Deploy Applications
  vars:
    - machine: development
  hosts: "{{ machine }}"
  gather_facts: no
  roles:
    - { role: app, become: yes }
  vars:
    - env: "development"
  # vars_files:
  #   - vars/xxx.yml